apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: helm-qdrant
spec:
  package: ghcr.io/hops-ops/helm-qdrant:{{ .Values.version }}
{{- if .Values.skipDependencyResolution }}
  skipDependencyResolution: true
{{- end }}
{{- if .Values.skipDependencyResolution }}
---
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: hops-ops-helm-qdrantrender
spec:
  package: ghcr.io/hops-ops/helm-qdrantrender:{{ .Values.version }}
{{- end }}
